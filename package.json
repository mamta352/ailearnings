{
  "scripts": {
    "build:js": "cat src/icons.jsx app.jsx > dist/combined.jsx && babel dist/combined.jsx --presets @babel/preset-react -o dist/app.js && terser dist/app.js -o dist/app.js --compress --mangle && rm dist/combined.jsx",
    "build:css": "tailwindcss -i tailwind.css -o dist/app.css --minify",
    "build:inline": "node scripts/inline.js",
    "build": "npm run build:js && npm run build:css && npm run build:inline",
    "snap": "cp index.html 200.html && rm -f 404.html"
  },
  "devDependencies": {
    "@babel/cli": "^7.28.6",
    "@babel/core": "^7.29.0",
    "@babel/preset-react": "^7.28.5",
    "@tailwindcss/cli": "^4.2.1",
    "lucide-react": "^0.577.0",
    "react-snap": "^1.23.0",
    "tailwindcss": "^3.4.19",
    "terser": "^5.46.0"
  }
}
